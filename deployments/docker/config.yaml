server:
  port: 8080

postgres:
  dsn: postgres://sudoku:sudoku@localhost:5432/sudoku_dev?sslmode=disable
  max_conns: 25
  min_conns: 5

redis:
  addr: localhost:6379
  password: ""
  db: 0
  pool_size: 10

rabbitmq:
  url: amqp://guest:guest@localhost:5672/

telemetry:
  otlp_endpoint: ""

log_level: debug
